
**Перечень основных команд запуска команд**
```bash
python main.py --help
```

**Загрузка и установка пакетов для работы backend**
```bash
mkdir packages && pip download -r requirements.txt -d packages
pip install -r requirements.txt --no-index --find-links packages
```

**Загрузка и установка пакетов для работы frontend**
```bash
echo "Их пока нет"
```